# Internationalization

English only, no i18n library. All copy is hardcoded in the page
components. Tracked as a future item in `ROADMAP.md`; `next-intl` is
the most Next.js-App-Router-native option if this becomes a priority.
